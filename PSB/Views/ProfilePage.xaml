<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PSB.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSB.Views"
    xmlns:utils="using:PSB.Utils"
    xmlns:rescol="using:PSB.Api.Response"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
            <!-- Контент (аватарка и ник) -->
        <StackPanel Orientation="Horizontal" Height="200" VerticalAlignment="Center" Spacing="12" Padding="36,0,0,0">
            <StackPanel.Background>
                <ImageBrush ImageSource="{x:Bind ProfileViewModel.User.Header, Mode=OneWay}"/>
            </StackPanel.Background>
            <Image  Stretch="Fill" Height="95" Width="95" Source="{x:Bind ProfileViewModel.User.Avatar, Mode=OneWay}"/>
            <TextBlock Text="{x:Bind ProfileViewModel.User.Nickname, Mode=OneWay}" FontWeight="SemiBold" FontSize="28" VerticalAlignment="Center"/>
        </StackPanel>
        <!-- Кнопки -->
        <StackPanel Height="75" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="0,0,16,0" Spacing="8">
            <Button Command="{x:Bind ProfileViewModel.LoadLibraryCommand, Mode=OneTime}" Content="Библиотека" Height="40" Width="110"/>
            <Button Command="{x:Bind ProfileViewModel.LogoutCommand, Mode=OneTime}" Content="Выйти" Height="40" Background="DarkRed" Width="130"/>
        </StackPanel>

    </StackPanel>
        
</Page>
