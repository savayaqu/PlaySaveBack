<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PSB.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSB.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel Orientation="Vertical">
        <TextBox PlaceholderText="Электронная почта" MaxHeight="50" Text="{x:Bind ViewModel.Identifier, Mode=TwoWay}"/>
        <TextBox PlaceholderText="Пароль" MaxHeight="50" Text="{x:Bind ViewModel.Password, Mode=TwoWay}"/>
        <TextBlock Text="{x:Bind ViewModel.Error, Mode=TwoWay}" />
        <Button Content="Войти" Command="{x:Bind ViewModel.LoginCommand, Mode=OneWay}"/>
        <Button Content="Создать аккаунт"/>
    </StackPanel>
</Page>
