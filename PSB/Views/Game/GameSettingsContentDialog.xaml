<ContentDialog
    x:Class="PSB.ContentDialogs.GameSettingsContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSB.ContentDialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Title="{x:Bind GameSettingsContentViewModel.GameName, Mode=OneWay}"
    PrimaryButtonText="Сохранить"
    SecondaryButtonText="Отмена"
    DefaultButton="Primary"
    Closing="ContentDialog_Closing">

    <StackPanel Spacing="10">
        <TextBlock Text="Файл" FontWeight="Bold"/>
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
            <TextBox Grid.Column="0" IsReadOnly="True" Text="{x:Bind GameSettingsContentViewModel.SelectedFile, Mode=OneWay}"/>
            <Button  Grid.Column="1" Content="Выбрать" Command="{x:Bind GameSettingsContentViewModel.ChooseFileCommand}"/>
        </Grid>

        <TextBlock Text="Сохранения" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal">
            
        </StackPanel>
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
            <TextBox Grid.Column="0" IsReadOnly="True" Text="{x:Bind GameSettingsContentViewModel.SelectedSavesFolder, Mode=OneWay}"/>
            <Button Grid.Column="1" Content="Выбрать" Command="{x:Bind GameSettingsContentViewModel.ChooseFolderSavesCommand}"/>
        </Grid>
        <TextBlock Text="Поведение сохранений" FontWeight="Bold"/>
        <TextBlock Text="Создавать новые сохранения (до 5 максимум) после изменения сохранений или перезаписывать. Сохранение ручное или автоматическое."/>

        <TextBlock Text="Опасная зона" FontWeight="Bold" Foreground="{StaticResource AccentAcrylicBackgroundFillColorDefaultBrush}"/>
        <StackPanel Orientation="Horizontal">
            <Button Content="Удалить из библиотеки" Background="{StaticResource AccentAcrylicBackgroundFillColorDefaultBrush}" Command="{x:Bind GameSettingsContentViewModel.RemoveFromLibraryCommand}"/>
            <Button Content="Удалить все сохранения" Background="{StaticResource AccentAcrylicBackgroundFillColorDefaultBrush}" Command="{x:Bind GameSettingsContentViewModel.RemoveAllSavesCommand}"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>