<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="PSB.ContentDialogs.GameSettingsContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSB.ContentDialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Title="{x:Bind GameSettingsContentViewModel.GameName, Mode=OneWay}"
    PrimaryButtonText="Закрыть"
    SecondaryButtonText="Выбрать"
    DefaultButton="Primary">
    <StackPanel Spacing="10">
        <Image Source="{x:Bind GameSettingsContentViewModel.ExeImage, Mode=OneWay}" Height="64" Width="64"/>
        <!--<ImageIcon Source="{x:Bind GameSettingsContentViewModel.ExeIcon, Mode=OneWay}"/>-->
        <TextBlock Text="Файл" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal">
            <TextBox IsReadOnly="True" Text="{x:Bind GameSettingsContentViewModel.SelectedFile, Mode=OneWay}"/>
            <Button Content="Выбрать" Command="{x:Bind GameSettingsContentViewModel.ChooseFileCommand}"/>
        </StackPanel>

        <TextBlock Text="Сохранения" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal">
            <TextBox IsReadOnly="True" Text="{x:Bind GameSettingsContentViewModel.SelectedSavesFolder, Mode=OneWay}"/>
            <Button Content="Выбрать" Command="{x:Bind GameSettingsContentViewModel.ChooseFolderSavesCommand}"/>
        </StackPanel>

        <TextBlock Text="Поведение сохранений" FontWeight="Bold"/>
        <TextBlock Text="Создавать новые сохранения (до 5 максимум) после изменения сохранений или перезаписывать. Сохранение ручное или автоматическое."/>

        <TextBlock Text="Опасная зона" FontWeight="Bold" Foreground="Red"/>
        <StackPanel Orientation="Horizontal">
            <Button Content="Удалить из библиотеки" Background="LightCoral" Command="{x:Bind GameSettingsContentViewModel.RemoveFromLibraryCommand}"/>
            <Button Content="Удалить все сохранения" Background="LightCoral" Command="{x:Bind GameSettingsContentViewModel.RemoveAllSavesCommand}"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>
