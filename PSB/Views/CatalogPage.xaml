<Page
    x:Class="PSB.Views.CatalogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSB.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GridView Grid.Column="0" ItemsSource="{x:Bind CatalogViewModel.Games, Mode=OneWay}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ScrollViewer.HorizontalScrollMode="Disabled"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">

            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="200" Margin="10"
                                HorizontalAlignment="Center"
                                Tapped="OnGameTapped">
                        <Image Source="{Binding LibraryImg}" Width="200" Height="150" Stretch="UniformToFill"/>
                        <TextBlock Text="{Binding Name}" FontSize="16" TextAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <!-- Пагинация -->
        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="1" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Command="{x:Bind CatalogViewModel.PreviousPageCommand}" Margin="0,0,10,0">
                    <FontIcon Glyph="&#xE892;" />
                </Button>
                <TextBlock Text="{x:Bind CatalogViewModel.CurrentPage, Mode=OneWay}" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Text="/" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Text="{x:Bind CatalogViewModel.TotalPages, Mode=OneWay}" VerticalAlignment="Center" Margin="10,0"/>
                <Button Command="{x:Bind CatalogViewModel.NextPageCommand}" Margin="10,0,0,0">
                    <FontIcon Glyph="&#xE893;" />
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock Text="Jump to " VerticalAlignment="Center" />
                <TextBox Text="{x:Bind CatalogViewModel.PageInput, Mode=TwoWay}" Width="50"/>
                <Button Content="GO" Command="{x:Bind CatalogViewModel.JumpToPageCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
