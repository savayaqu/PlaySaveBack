<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PSB.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSB.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:PSB.ViewModels"
    xmlns:models="using:PSB.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопка обновления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Padding="10">
            <TextBlock Text="Локальные сохранения" Style="{StaticResource TitleTextBlockStyle}"/>
            <Button Content="Обновить" Command="{x:Bind HomeViewModel.LoadLocalSavesCommand, Mode=OneWay}"/>
        </StackPanel>

        <!-- Индикатор загрузки -->
        <ProgressRing Grid.Row="1" 
                      IsActive="{x:Bind HomeViewModel.IsLoading, Mode=OneWay}"
                      Visibility="{x:Bind HomeViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=False}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Width="50" Height="50"/>

        <!-- Список сохранений -->
        <ScrollViewer>
            <ItemsControl ItemsSource="{x:Bind HomeViewModel.LocalSaves, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:GroupedSaves">
                        <Expander>
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <SymbolIcon Symbol="Placeholder"/>
                                    <TextBlock Text="{x:Bind GameName}" 
                                       FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind Saves.Count}" 
                                       Foreground="Gray"/>
                                </StackPanel>
                            </Expander.Header>
                            <ListView ItemsSource="{x:Bind Saves}" 
                              SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:Save">
                                        <StackPanel Orientation="Horizontal" Spacing="8" Padding="12,8">
                                            <SymbolIcon Symbol="Save"/>
                                            <TextBlock Text="{x:Bind FileName}"/>
                                            <TextBlock Text="{x:Bind CreatedAt}" 
                                               Foreground="Gray"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>



    </Grid>
</Page>